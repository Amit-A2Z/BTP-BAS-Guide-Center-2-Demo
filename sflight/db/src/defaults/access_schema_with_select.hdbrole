{
  "role": {
    "name": "ROLE_SELECT",
    "schema_privileges": [
      {
        "privileges": [
          "CREATE TEMPORARY TABLE",
          "EXECUTE",
          "SELECT",
          "SELECT CDS METADATA",
          "SELECT METADATA"
        ]
      }
    ]
  }
}
