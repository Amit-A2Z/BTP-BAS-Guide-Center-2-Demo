{
  "role": {
    "name": "ROLE_SELECTWITHGRANT#",
    "schema_privileges": [
      {
        "privileges": [
          "CREATE TEMPORARY TABLE",
          "EXECUTE",
          "SELECT",
          "SELECT CDS METADATA",
          "SELECT METADATA"
        ],
        "privileges_with_grant_option": [
          "CREATE TEMPORARY TABLE",
          "EXECUTE",
          "SELECT",
          "SELECT CDS METADATA",
          "SELECT METADATA"
        ]
      }
    ]
  }
}
